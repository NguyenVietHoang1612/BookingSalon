@using BookingSalon.Models.Entities
@model FixedTimeSlot

@{
    ViewData["title"] = "Cập nhật chi nhánh";
}

<div class="container-fluid py-5 px-md-5">
    <div class="card main-card p-4 p-lg-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold text-primary">Quản lý Khung giờ </h2>
        </div>

        <div class="form-section">
            <h5 class="fw-bold mb-4 text-dark"><i class="bi bi-person-plus me-2"></i>Cập nhật khung giờ</h5>

            <form asp-action="Update">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small mb-4"></div>

                <div class="row g-4">
                    <div class="col-12 col-md-6 col-lg-3">
                        <label class="form-label fw-semibold">Khung giờ trong ngày</label>
                        <input type="time" asp-for="TimeLabel" class="form-control" placeholder="Nhập giờ" />
                        <span asp-validation-for="TimeLabel" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="col-12 col-md-6 col-lg-3">
                        <label class="form-label fw-semibold">Sắp xếp khung giờ</label>
                        <input type="number" asp-for="Sort_Order" class="form-control" placeholder="Nhập số thứ tự xuất hiện" />
                        <span asp-validation-for="TimeLabel" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="col-12 d-flex">
                        <button type="submit" class="btn btn-primary btn-add fw-bold">
                            <i class="bi bi-plus-lg me-2"></i> Hoàn tất cập nhật
                        </button>

                        <a asp-action="Index" class="btn btn-danger fw-bold">
                            <i class="bi bi-x-lg me-2">Hủy cập nhật</i>
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
