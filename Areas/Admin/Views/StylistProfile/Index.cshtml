@using BookingSalon.Models.Entities
@using BookingSalon.Data.Repository
@model PaginatedList<StylistProfile>

@{
    ViewData["title"] = "Danh dách stylist profile";
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}


<div class="container-fluid py-2 px-md-5">
    <div class="card main-card p-2 p-lg-4">
        <div class="row align-items-center mb-5">
                <div class="col-xl-6 col-lg-5">
                    <h2 class="title-main mb-lg-0 mb-3">Quản lý loại dịch vụ</h2>
                </div>
                <div class="col-xl-6 col-lg-7">
                    <div class="row g-3 justify-content-lg-end">
                        <div class="col-sm-7 col-12">
                            <form asp-action="Index" method="get">
                                <div class="input-group search-group">
                                    <input type="text" name="term" value="@ViewBag.SearchTerm" class="form-control" placeholder="Tìm kiếm theo tên loại dịch vụ...">
                                    <button type="submit" class="btn border"><i class="bi bi-search"></i></button>
                                </div>
                            </form>

                        </div>
                        <div class="col-sm-5 col-12 d-grid">
                            <a class="btn btn-primary shadow-sm text-decoration-none text-white" asp-action="Create">
                                <i class="bi bi-plus-lg me-2"></i> 
                                Thêm loại dịch vụ
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th class="text-start">Id</th>
                            <th class="text-start">Stylist</th>
                            <th class="text-start">Branch</th>
                            <th class="text-start">Bio</th>
                            <th class="text-start">Kinh nghiệm</th>
                            <th class="text-start">Giờ bắt đầu và kết thúc làm việc trong ngày</th>
                            <th class="text-center">Hành động</th>
                            @* <th class="text-center"></th>
                            <th class="text-end pe-4"></th>
                            <th class="text-end pe-4"></th> *@
                        </tr>
                    </thead>
                    <tbody id="userTableBody">
                        @foreach (var item in Model)
                        {
                            <tr>
                            <td class="text-muted">@item.StylistId</td>
                                <td class="text-muted">@item.Stylist.FullName</td>
                                <td class="text-muted">@item.Branch.Branch_Name</td>
                                <td class="text-muted">@item.StylistSkill</td>
                                <td class="text-muted">@item.StylistExperience</td>
                                <td class="text-muted">@item.Start_Work_Time <i> - </i> @item.End_Work_Time</td>

                                <td class="text-center pe-4">
                                    <a asp-action="Update" asp-route-id="@item.StylistId"><i class="bi bi-pencil-square action-btn"></i></a>

                                <form id="delete-form-@item.StylistId" asp-action="Delete" asp-route-id="@item.StylistId" method="post" style="display:inline;">
                                    <button type="button" class="btn-clean" onclick="confirmDelete('@Url.Action("Delete", "StylistProfile", new { id = item.StylistId })', '@item.Stylist.FullName')">
                                            <i class="bi bi-trash-fill text-danger action-btn"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        <div class="row align-items-center mt-3">
            <div class="col-sm-6 text-muted">
                Hiển thị trang <b>@Model.PageIndex</b> / Trong tổng số <b>@Model.TotalPages</b> trang
            </div>
            <div class="col-sm-6">
                <nav aria-label="Page navigation">
                    <ul class="pagination justify-content-end mb-0">

                        <li class="page-item @prevDisabled">
                            <a class="page-link" asp-action="Index" asp-route-pageNumber="1" asp-route-term="@ViewBag.SearchTerm">
                                <i class="bi bi-chevron-double-left"></i>
                            </a>
                        </li>

                        <li class="page-item @prevDisabled">
                            <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.PageIndex - 1)" asp-route-term="@ViewBag.SearchTerm">
                                <i class="bi bi-chevron-left"></i>
                            </a>
                        </li>

                        @for (int i = 1; i <= Model.TotalPages; i++)
                        {
                            // Logic để chỉ hiển thị một vài số trang nếu tổng số quá lớn
                            if (i >= Model.PageIndex - 2 && i <= Model.PageIndex + 2)
                            {
                                <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                                    <a class="page-link" asp-action="Index" asp-route-pageNumber="@i" asp-route-term="@ViewBag.SearchTerm">@i</a>
                                </li>
                            }
                        }

                        <li class="page-item @nextDisabled">
                            <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.PageIndex + 1)" asp-route-term="@ViewBag.SearchTerm">
                                <i class="bi bi-chevron-right"></i>
                            </a>
                        </li>

                        <li class="page-item @nextDisabled">
                            <a class="page-link" asp-action="Index" asp-route-pageNumber="@Model.TotalPages" asp-route-term="@ViewBag.SearchTerm">
                                <i class="bi bi-chevron-double-right"></i>
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>

@section Styles {
        <link rel="stylesheet" href="~/css/Paginated.css" />
}
